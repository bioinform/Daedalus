process {
    withName:  ipete_dedup {
        memory = '40 GB'
        cpus = 1 
        module = [
            'miniconda3'
	]      
        ext {
            command = {"$workflow.projectDir/config/ipete_dedup/ipete_dedup.sh"}
            vmem = '40G'
        }
    }
}
